# Motor start/stop

When `Start` is true and `Stop` is false, `Motor` must be true. When `Stop`
is true, `Motor` must be false. The complete latch behavior will be exercised
after the runtime and behavior-verification milestones are implemented.
